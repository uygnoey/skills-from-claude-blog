# Note on `claude-api skill` mention

The blog post notes that developers can use the latest version of Claude Code and the built-in command:

```
claude-api skill
```

Then prompt Claude with:

> start onboarding for managed agents in Claude API

This repository does not reproduce the upstream `claude-api` implementation; it only preserves the invocation and onboarding prompt that the post explicitly mentions.
