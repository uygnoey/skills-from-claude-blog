# Managed Agents onboarding prompt (from the blog post)

Use this as a starting point prompt in Claude Code after invoking the built-in `claude-api skill`.

Prompt to send:

> start onboarding for managed agents in Claude API
